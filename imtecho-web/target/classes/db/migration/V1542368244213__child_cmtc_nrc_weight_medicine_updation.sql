alter table child_cmtc_nrc_admission_detail
drop column if exists child_stay_duration,
drop column if exists is_mother_councelling,
drop column if exists mother_councelling_days,
drop column if exists is_amoxicillin,
drop column if exists is_vitamina,
drop column if exists is_albendazole,
drop column if exists is_folic_acid,
drop column if exists is_potassium,
drop column if exists is_magnesium,
drop column if exists is_zinc,
drop column if exists is_iron;

alter table child_cmtc_nrc_weight_detail
drop column if exists child_stay_duration,
drop column if exists is_mother_councelling,
drop column if exists mother_councelling_days,
drop column if exists is_amoxicillin,
drop column if exists is_vitamina,
drop column if exists is_albendazole,
drop column if exists is_folic_acid,
drop column if exists is_potassium,
drop column if exists is_magnesium,
drop column if exists is_zinc,
drop column if exists is_iron,
add column child_stay_duration integer,
add column is_mother_councelling boolean,
add column mother_councelling_days text,
add column is_amoxicillin boolean,
add column is_vitamina boolean,
add column is_albendazole boolean,
add column is_folic_acid boolean,
add column is_potassium boolean,
add column is_magnesium boolean,
add column is_zinc boolean,
add column is_iron boolean;